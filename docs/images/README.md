Drop preview assets here if you want them linked from the root README, for example:

- `chat-overview.png` — main chat + commentator panel
- `workshop-mode.png` — 0→1 workshop flow (optional)

GitHub paths in README use `docs/images/<filename>`.
